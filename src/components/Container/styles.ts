import { styled } from '~/styles/stitches.config'

export const Wrapper = styled('div', {
  width: '100%',
  maxWidth: '1248px',
  padding: '0 1.5rem',

  display: 'flex',
  flexDirection: 'column',
})
